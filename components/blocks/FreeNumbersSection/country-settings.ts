export default {
  7: {
    flagSrc: "/static/emojione-v1_flag-for-russia.png",
    regexp: /^(\d{3})(\d{3})(\d{2})(\d{2})/,
    mask: "($1) $2-$3-$4",
  },
  77: {
    flagSrc: "/static/twemoji_flag-for-flag-kazakhstan.png",
    regexp: /^(\d{3})(\d{3})(\d{3})/,
    mask: "($1) $2-$3",
  },
  380: {
    flagSrc: "/static/twemoji_flag-for-flag-ukraine.png",
    regexp: /^(\d{3})(\d{3})(\d{3})/,
    mask: "($1) $2-$3",
  },
  63: {
    flagSrc: "/static/philippines.png",
    regexp: /^(\d{3})(\d{3})(\d{3})/,
    mask: "($1) $2-$3",
  },
  44: {
    flagSrc: "/static/twemoji_flag-for-flag-united-kingdom.png",
    regexp: /^(\d{3})(\d{3})(\d{3})/,
    mask: "($1) $2-$3",
  },
}
